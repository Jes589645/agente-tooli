# Agente TOOLI-UTB (MVP)

## Requisitos
- Docker y Docker Compose
- Clave de API LLM OpenAI-compatible (p.ej., Groq): exporta `LLM_API_KEY`

## Configuración
1) Copia `.env.example` a `.env` y define:
   - LLM_API_KEY=gsk_********
   - (opcional) LLM_BASE_URL, LLM_MODEL

2) Construye y ejecuta:
